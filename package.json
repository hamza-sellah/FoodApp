{
    "name": "foodapp",
    "version": "1.0.0",
    "scripts": {
                "start": "cd backendapp/built && node server.js",
        "prebuild": "cd backendapp && npm run build",
        "build": "cd frontendapp && npm run build"
    }

}
